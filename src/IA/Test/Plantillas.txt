const BacklogEUT = () => {
	const data = new google.visualization.DataTable();
	data.addColumn('string', 'ID');
	data.addColumn('string', 'Parent');
	data.addColumn('number', 'Total');
	data.addColumn('number', 'Nuevo');
	data.addColumn('number', 'Abierto');
	data.addColumn('number', 'En Curso');
	data.addColumn('number', 'En Espera');
	data.addRows(dat04);

	const tree = new google.visualization.TreeMap(chartR04);

	const options = {
		enableHighlight: true,
		maxDepth: 1,
		maxPostDepth: 2,
		minHighlightColor: '#fa3939',
		midHighlightColor: '#753f3f',
		maxHighlightColor: '#575757',
		minColor: '#575757',
		midColor: '#995757',
		maxColor: '#fa3939',
		headerHeight: 15,
		showScale: true,
		generateTooltip: showFullTooltip,
		height: 600,
		useWeightedAverageForAggregation: true,
		titleTextStyle: {
			font: 16,
			color: '#ffffff',
		},
		legend: {
			textStyle: {
				color: '#ffffff',
			},
		},
		// Use click to highlight and double-click to drill down.
		eventsConfig: {
			highlight: ['mousein'],
			unhighlight: ['mouseout'],
			rollup: ['dblclick'],
			drilldown: ['click'],
		},
	};

	tree.draw(data, options);
	function showFullTooltip(row, size, value) {
		const rowData = data.getValue(row, 0);
		const TotalValue = data.getValue(row, 2);
		const nuevoValue = data.getValue(row, 3);
		const abiertoValue = data.getValue(row, 4);
		const cursoValue = data.getValue(row, 5);
		const pendienteValue = data.getValue(row, 6);
		const link = `http://180.176.105.244/eut_api_reportes/rutes/reportseut.php?download=${rowData.replace(/,/g, '').replace(/ /g, '_')}`;

		return `
      <div style="background:#272B30; padding:10px; border-style:none">
        <span style="font-family:Verdana"><b>${rowData}</b>,</span><br>
        Total: ${TotalValue}<br>
        ${data.getColumnLabel(3)}: ${nuevoValue}<br>
        ${data.getColumnLabel(4)}: ${abiertoValue}<br>
        ${data.getColumnLabel(5)}: ${cursoValue}<br>
        ${data.getColumnLabel(6)}: ${pendienteValue}<br>
        <a href=${link}><button type="button" class="btn">Descargar</button></a><br>
      </div>
    `;
	}
};