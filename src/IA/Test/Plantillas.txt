$userPath = [Environment]::GetEnvironmentVariable("Path","User")
if (-not ($userPath -split ';' | Where-Object { $_ -eq 'C:\node' })) {
  [Environment]::SetEnvironmentVariable("Path", $userPath + ';C:\node', "User")
}
Write-Host "Listo. Abre una nueva consola."